include "main.h"
/**
 * _islower - run code
 * @c:int
 * Description: checks if a character is lowercase
 * Return: returns 1 or 0
 */
int _islower(int c)
{
if (c > 96 && c < 123)
return (1);
else
return (0);
}
